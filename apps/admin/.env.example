# Copy to apps/admin/.env. All PUBLIC_* are inlined at build time.
PUBLIC_SUPABASE_URL=http://127.0.0.1:55321
PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# C1 central gateway — the admin's privileged read/write target (W1).
PUBLIC_GATEWAY_URL=http://127.0.0.1:8787
